# V1.15 设计:简历素材打包

日期:2026-08-14
版本:V1.15
前置:V1.14(前端 Agent 进度面板)

## 1. 背景与目标

V1.1-V1.14 已完成 14 个版本的 Agent 系统建设,但**求职素材没有跟上**:README 版本历史停在 V1.7,简历亮点只覆盖早期能力(证据驱动/审批/工具层/真实系统),V1.8-V1.14 的记忆/反思/路由/评测/可视化等核心能力没有沉淀成可展示的卖点。对照"写简历找工作"的目标,缺的是**把积累变成面试官能看懂、能记住的成果**。

本版本目标:**把 V1.1-V1.14 的全部能力沉淀成可直接用于求职的素材**——README 版本历史补全 + 简历亮点升级(带量化证据)+ 面试 Q&A 文档 + 项目一句话描述。

## 2. 现状盘点

- **README**(364 行):架构总览/快速开始/演示流程/目录结构/技术栈/简历亮点(4 条,停在早期)/版本历史(停在 V1.7)。
- **量化数据**(真实):后端 422 个测试函数;SCN-001/002 多场景;真实模型验收通过;单次诊断成本 ¥0.0153(V1.11/1.12 实测);离线评测 24+ 条;前端 45 测试。
- **可复用**:docs/architecture.md、docs/demo-script.md、14 份 spec、7 份 plan。

## 3. 块 1:README 版本历史补全(V1.8 → V1.14)

在现有 V1.7 章节后追加:

| 版本 | 内容(2-3 句 + 量化) |
|---|---|
| V1.8 | Agent 运行观测面板 + 量化评测报告(成功率/耗时/tokens) |
| V1.9 | 长期记忆(qdrant 案例沉淀 + 语义检索复用)+ 上下文压缩(EvidenceSummarizer) |
| V1.10 | 反思自改进(reflect 节点结构化复盘 + 3 轮重试)+ 失败案例负样本记忆(避坑检索) |
| V1.11 | 多模型路由(节点级选模型)+ 成本统计(单次诊断 ¥0.0153)+ 容灾 fallback |
| V1.12 | 动态路由学习(窗口滚动评分,成功率/时延/成本加权) |
| V1.13 | 评测平台可视化(eval_run 持久化 + 前端列表/详情/趋势) |
| V1.14 | Agent 进度面板(SSE 事件前端实时展示,节点级渐进可视化) |

## 4. 块 2:简历亮点升级(重写现有 4 条)

三层结构,每条带量化证据:

**工程能力层**:
- 证据驱动诊断闭环(LangGraph 状态机 + E1~E5/L1~L6 事实闸门,消除 LLM 幻觉)+ 双策略多场景 + human-in-the-loop 审批安全闭环
- MCP 协议标准化(stdio→Streamable HTTP)+ 最小权限隔离(五账号/三连接池)+ 全量审计与回放
- 真实可观测性(Prometheus/Jaeger/OTel)+ SSE 实时进度面板

**Agent 智能层**:
- 长期记忆(qdrant 向量沉淀 + 语义检索)+ 上下文压缩
- 反思自改进(失败→结构化复盘→重试,负样本避坑记忆)
- 多模型路由 + 动态路由学习(数据驱动选模型)

**数据验证层**:
- 后端 422 测试 / 前端 45 测试 / 24+ 离线评测 / SCN-001/002 真实模型验收
- 成本量化:单次诊断 ¥0.0153(多模型路由实测)
- 评测平台可视化:成功率/耗时/成本趋势

## 5. 块 3:面试 Q&A(docs/interview-qa.md 新建)

8-10 个高频问题,每个答案 3-5 句紧扣项目落地:

1. 这个项目解决了什么问题?(电梯陈述)
2. Agent 怎么防幻觉?(证据闸门 + 事实数据源)
3. 多 Agent 怎么设计?(单 Agent 演进视角 + 反思/记忆/路由分层)
4. 怎么验证 Agent 改进有效?(量化评测 + 成本数据)
5. RAG 检索不到怎么办?(降级 + 记忆负样本 + 真实数据源)
6. 模型成本怎么控?(节点级路由 + 动态学习 + 成本账单)
7. human-in-the-loop 怎么设计?(审批节点 + 唯一写路径)
8. 工具层为什么用 MCP?(标准化 + 权限隔离 + 审计)
9. 长上下文怎么处理?(EvidenceSummarizer 压缩)
10. 测试怎么做的?(TDD + FakeEngine + 真实模型验收)

## 6. 块 4:项目一句话描述(README 顶部)

40 字内电梯陈述,可贴简历"项目经历"栏:

> 示例:基于 LangGraph 的 AI 故障诊断系统:证据驱动消除幻觉,多模型路由控成本,记忆+反思持续进化,评测平台量化验证改进。

## 7. 测试与验收

- 文档类改动,无代码测试;验收 = 人工 review 素材质量。
- 检查:README 版本历史 V1.1-V1.14 完整;简历亮点带量化证据;interview-qa.md 8-10 题;一句话描述 ≤40 字。
- 提交并推送(网络恢复时)。

## 8. 范围边界(明确不做)

- 不做新的系统功能(纯文档/素材)。
- 不改代码、不改现有 README 的架构/快速开始部分(只补版本历史 + 升级简历亮点 + 顶部一句话)。

## 9. 简历价值

1. **成果可展示**:14 个版本的积累变成面试官 5 分钟能看懂的卖点
2. **量化证据贯穿**:测试数/评测数/成本数据/真实模型验收
3. **面试弹药**:8-10 个高频问题的项目化答案,直接可背
